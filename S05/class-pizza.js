const input = input('prompt-sync')

let borda

let preco

calcularPreco(tamanho, borda) {
   if(tamanho==P) {
        console.log('o preço é R$ 25')
    } else if(tamanho==M) {
        console.log('o preço é R$ 35')
    } else if(tamanho==G) {
        console.log('o preço é R$ 50')
    }


class Pizza {
    constructor(sabor, tamanho, borda) {
        this.sabor = sabor
        this.tamanho = tamanho
        this.borda = borda
    }
    opcaoTamanho() {
        console.log(`vc escolheu a pizza ${tamanho}`)
    }
    temBorda() {
        if borda = true
        console.log('tem um acréscimo de R$ 8')
    }
    }
    resumo(){

    }
}

// não terminado e provavelment não funciona
//  > refazer